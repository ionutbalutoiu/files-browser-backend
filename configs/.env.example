# Files Service Configuration
#
# Base directory for file storage (required for serving files)
# Default: /srv/files
FILES_SVC_BASE_DIR=/path/to/files

# Public base directory for shared files (optional)
# When set, enables public file sharing via symlinks
# Default: empty (public sharing disabled)
FILES_SVC_PUBLIC_BASE_DIR=/path/to/public

# Maximum upload size in bytes
# Default: 2147483648 (2GB)
FILES_SVC_MAX_UPLOAD_SIZE=104857600
